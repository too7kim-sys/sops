<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans     https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context    https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/task       https://www.springframework.org/schema/task/spring-task.xsd">

    <!-- 전역 프로퍼티 (시스템속성/환경변수 포함) -->
    <context:property-placeholder location="classpath:egovframework/globals.properties"
                                  ignore-unresolvable="true" system-properties-mode="OVERRIDE"/>

    <!-- 컴포넌트 스캔 : Business/Service/Config (Controller 는 dispatcher 컨텍스트에서 스캔) -->
    <context:component-scan base-package="egovframework">
        <context:exclude-filter type="annotation"
                                 expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <!-- 비밀번호 암호화 (BCrypt) -->
    <bean id="passwordEncoder"
          class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <!-- 메시지 소스 -->
    <bean id="messageSource"
          class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basenames" value="classpath:messages/message"/>
        <property name="defaultEncoding" value="UTF-8"/>
        <property name="cacheSeconds" value="60"/>
    </bean>

    <!-- 비동기 실행 (Git 자동배포 @Async) -->
    <task:annotation-driven executor="deployTaskExecutor"/>
    <task:executor id="deployTaskExecutor" pool-size="2-5" queue-capacity="20"/>

</beans>
