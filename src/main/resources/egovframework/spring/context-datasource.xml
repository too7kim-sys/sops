<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/jdbc  https://www.springframework.org/schema/jdbc/spring-jdbc.xsd">

    <!-- ===== 개발/데모 : H2 In-memory (기본 프로파일) ===== -->
    <beans profile="dev,default">
        <bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close">
            <property name="driverClassName" value="org.h2.Driver"/>
            <property name="url" value="jdbc:h2:mem:egovsop;MODE=Oracle;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE"/>
            <property name="username" value="sa"/>
            <property name="password" value=""/>
        </bean>

        <!-- 스키마/초기데이터 적재 (H2) -->
        <jdbc:initialize-database data-source="dataSource" enabled="true">
            <jdbc:script location="classpath:db/schema.sql" encoding="UTF-8"/>
            <jdbc:script location="classpath:db/data.sql" encoding="UTF-8"/>
        </jdbc:initialize-database>
    </beans>

    <!-- ===== 운영 : PostgreSQL ===== -->
    <beans profile="prod">
        <bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close">
            <property name="driverClassName" value="org.postgresql.Driver"/>
            <property name="url" value="${DB_URL:jdbc:postgresql://localhost:5432/egovsop}"/>
            <property name="username" value="${DB_USERNAME:egovsop}"/>
            <property name="password" value="${DB_PASSWORD:egovsop}"/>
            <property name="maxTotal" value="${DB_POOL_MAX:20}"/>
            <property name="initialSize" value="5"/>
        </bean>

        <!-- 비파괴적 스키마/기준데이터 (멱등) -->
        <jdbc:initialize-database data-source="dataSource" enabled="${SQL_INIT_ENABLED:true}">
            <jdbc:script location="classpath:db/postgresql/schema.sql" encoding="UTF-8"/>
            <jdbc:script location="classpath:db/postgresql/data.sql" encoding="UTF-8"/>
        </jdbc:initialize-database>
    </beans>

</beans>
