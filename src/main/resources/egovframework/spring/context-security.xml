<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
                 http://www.springframework.org/schema/beans    https://www.springframework.org/schema/beans/spring-beans.xsd
                 http://www.springframework.org/schema/security https://www.springframework.org/schema/security/spring-security.xsd">

    <http use-expressions="true">
        <intercept-url pattern="/login"        access="permitAll"/>
        <intercept-url pattern="/css/**"       access="permitAll"/>
        <intercept-url pattern="/js/**"        access="permitAll"/>
        <intercept-url pattern="/images/**"    access="permitAll"/>
        <intercept-url pattern="/h2-console/**" access="permitAll"/>
        <intercept-url pattern="/error"        access="permitAll"/>

        <!-- 부서 검색 팝업 : 결재선/공유 지정에 필요하므로 운영자도 허용 (/sys/** 보다 먼저) -->
        <intercept-url pattern="/sys/dept/popup" access="hasAnyRole('ADMIN','OPERATOR')"/>
        <!-- 사용자/공통코드 관리 : 운영관리자 -->
        <intercept-url pattern="/sys/**"       access="hasRole('ADMIN')"/>
        <!-- 결재선/공유 기본설정(템플릿) 관리 : 운영관리자 -->
        <intercept-url pattern="/appr/template/**" access="hasRole('ADMIN')"/>
        <intercept-url pattern="/appr/template"    access="hasRole('ADMIN')"/>
        <!-- Git 자동배포 실행 : 운영관리자/운영자 -->
        <intercept-url pattern="/release/deploy" method="POST" access="hasAnyRole('ADMIN','OPERATOR')"/>

        <intercept-url pattern="/**" access="isAuthenticated()"/>

        <form-login login-page="/login"
                    login-processing-url="/login"
                    username-parameter="userId"
                    password-parameter="password"
                    default-target-url="/main"
                    always-use-default-target="true"
                    authentication-failure-url="/login?error=true"/>
        <logout logout-url="/logout"
                logout-success-url="/login?logout=true"
                delete-cookies="JSESSIONID"/>

        <csrf disabled="true"/>
        <headers>
            <frame-options policy="SAMEORIGIN"/>
        </headers>
    </http>

    <authentication-manager>
        <authentication-provider user-service-ref="egovUserDetailsService">
            <password-encoder ref="passwordEncoder"/>
        </authentication-provider>
    </authentication-manager>

</beans:beans>
